{
  "image": "mcr.microsoft.com/vscode/devcontainers/python:3.12",
  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y tesseract-ocr poppler-utils && pip install --upgrade pip && pip install -r requirements.txt && python setup.py",
  "features": {
  },
  	// Container environment variables
	"containerEnv": {
		"ARTN_API_KEY": "${localEnv:ARTN_API_KEY}",
    "ARTN_API_BASE": "${localEnv:ARTN_API_BASE}"
	}
}
